name: Create if not exists
description: Create a resource if not exists using az subcommand
inputs:
  resourceName:
    description: Name of the resource
    required: true
  checkCmd:
    description: Command to check if resource exists
    required: true
  createCmd:
    description: Command to create the resource
    required: true
runs:
  using: composite
  steps:
    - name: Create resource if not exists
      shell: bash
      run: |
        set -euo pipefail

        # DEBUG: imprime el comando que se va a ejecutar (quita el echo en production)
        # echo "Checking resource '${{ inputs.resourceName }}' with:"
        # echo "  checkCmd: ${{ inputs.checkCmd }}"

        eval "${{ inputs.checkCmd }}"
        RESULT=$?
        echo "Check command exited with code: $RESULT"

        if [[ $RESULT -eq 0 ]]; then
          echo "⚠️ Resource '${{ inputs.resourceName }}' already exists. Skipping creation."
        else
          echo "🚀 Resource '${{ inputs.resourceName }}' not found. Creating..."

          eval "${{ inputs.createCmd }}"

          RESULT=$?
          if [[ $RESULT -eq 0 ]]; then
            echo "✅ Resource '${{ inputs.resourceName }}' created successfully."
          else
            echo "❌ Failed to create resource '${{ inputs.resourceName }}'. Exiting with code $RESULT."
            exit $RESULT
          fi
        fi