name: WF07 - Deploy Main Agent to Azure

on:
  workflow_dispatch:

jobs:
  dummy:
    runs-on: ubuntu-latest
    steps:
      - name: Do nothing
        run: echo "✅ Dummy job executed successfully"